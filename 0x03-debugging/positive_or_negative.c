#include "main.h"
/**
 * main - this is the main function
* i is the iariable that hold the result
 * Return: 0
 *
 */

/* more headers goes there */

void positive_or_negative(int i);
{
if (n > 0)
	printf("%d is positive\n", n);
	else if (n < 0)
	 printf("%d is negative\n", n);
	else
	printf("%d is zero\n", n);
}
